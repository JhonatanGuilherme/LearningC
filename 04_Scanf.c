#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int a;
    double b;
    scanf("%d %lf", &a, &b); // É possível solicitar entrada de uma ou mais variáveis por vez.

    system("pause");

    return (0);
}